import type { CountryLegislationPackage } from '../../registry/types';

/** Botswana — registered country; packages added when legislation is implemented. */
export const BOTSWANA_PACKAGES: readonly CountryLegislationPackage[] = [] as const;
