# Botswana

Registered in the country registry (`BW`). Add `years/<tax-year>/` packages and register them in `registry.ts` when Botswana legislation is implemented.

No payroll engine changes required.
