# Namibia

Registered in the country registry (`NA`). Add `years/<tax-year>/` packages and register them in `registry.ts` when Namibian legislation is implemented.

No payroll engine changes required.
