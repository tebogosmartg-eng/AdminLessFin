/**
 * IFRS framework pack 2026.1 — entry point.
 * Content lives in the shared pack library; this module declares the versioned pack identity.
 */
import { getFrameworkKnowledgePack } from '../../../registry';

export const IFRS_PACK_2026_1 = getFrameworkKnowledgePack('IFRS', '2026.1');
export default IFRS_PACK_2026_1;
