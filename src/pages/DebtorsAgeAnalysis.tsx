import AgeAnalysis from './AgeAnalysis';

/** Debtors (trade receivables) age analysis. See AgeAnalysis for the report. */
const DebtorsAgeAnalysis = () => <AgeAnalysis side="receivable" />;

export default DebtorsAgeAnalysis;
