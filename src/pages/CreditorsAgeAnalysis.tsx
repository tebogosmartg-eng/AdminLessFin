import AgeAnalysis from './AgeAnalysis';

/** Creditors (trade payables) age analysis. See AgeAnalysis for the report. */
const CreditorsAgeAnalysis = () => <AgeAnalysis side="payable" />;

export default CreditorsAgeAnalysis;
