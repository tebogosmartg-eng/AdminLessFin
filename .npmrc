# react-mentions@3.0.2 declares a react@16.x peer range and has not been updated
# for React 18. The dependency tree in package-lock.json is the verified working
# tree; without this flag `npm ci` aborts with ERESOLVE in CI and on Vercel.
legacy-peer-deps=true
