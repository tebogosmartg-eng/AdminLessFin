import type { CountryLegislationPackage } from '../../registry/types.ts';

/** Namibia — registered country; packages added when legislation is implemented. */
export const NAMIBIA_PACKAGES: readonly CountryLegislationPackage[] = [] as const;
